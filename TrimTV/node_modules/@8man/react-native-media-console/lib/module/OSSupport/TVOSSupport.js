import { useTVEventHandler } from 'react-native';
export const TVOSSupport = ({
  showControls,
  onScreenTouch
}) => {
  useTVEventHandler(() => {
    if (!showControls) {
      onScreenTouch();
    }
  });
  return null;
};
//# sourceMappingURL=TVOSSupport.js.map